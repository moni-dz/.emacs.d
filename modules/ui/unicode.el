;;; unicode.el --- make Unicode characters not suck -*- lexical-binding: t; -*-
;;; Commentary:
;; this fixes the small `org-superstar' icons
;;; Code:
(use-package unicode-fonts :init (unicode-fonts-setup))
;;; unicode.el ends here
