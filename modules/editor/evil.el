;;; evil.el --- Vim bindings for Emacs -*- lexical-binding: t; -*-
;;; Commentary:
;; For people who prefer having `vim' in Emacs
;;; Code:
(use-package evil :config (evil-mode +1))
;;; evil.el ends here
